# Resolved dependency lock for the zy_CNN experiment (Phase 3).
#
# Captured from the project venv on Windows with official CPython 3.12.5
# (win-amd64). Covers the base runtime plus the `train` and `dev` extras; the
# `app` extras (fastapi, uvicorn, streamlit, pydantic, httpx) are deliberately
# deferred to Phase 12.
#
# The torch/torchvision pins are CUDA 12.6 builds and are NOT on PyPI, so the
# extra index below is required. Reinstall with:
#
#   python -m pip install -r requirements-lock.txt
#
# Regenerate with:
#
#   python -m pip freeze --exclude-editable
#
# The project package itself is installed separately and on purpose is absent
# here:  python -m pip install -e . --no-deps
--extra-index-url https://download.pytorch.org/whl/cu126

ast_serialize==0.6.0
colorama==0.4.6
contourpy==1.3.3
coverage==7.15.2
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
iniconfig==2.3.0
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
librt==0.13.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
mypy==2.3.0
mypy_extensions==1.1.0
narwhals==2.24.0
networkx==3.6.1
numpy==2.4.4
packaging==26.2
pandas==3.0.5
pathspec==1.1.1
pillow==12.2.0
pluggy==1.6.0
Pygments==2.20.0
pyparsing==3.3.2
pytest==9.1.1
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
PyYAML==6.0.3
ruff==0.16.1
scikit-learn==1.9.0
scipy==1.18.0
setuptools==83.0.0
six==1.17.0
sympy==1.14.0
threadpoolctl==3.6.0
torch==2.13.0+cu126
torchvision==0.28.0+cu126
tqdm==4.70.0
typing_extensions==4.15.0
tzdata==2026.3
wheel==0.47.0
