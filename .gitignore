# macOS
.DS_Store

# Datasets - never commit images
IP102_v1.1/
data/

# Training outputs - never commit checkpoints
runs/
*.pt
*.pth

# Manifests are large and fully regenerable from the committed splits_top10.json
# (python scripts/setup_data.py). classes.json and norm_stats.json ARE committed:
# they are small, and everyone must normalize identically for the runs to be
# comparable.
data_manifests/*.csv

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/
